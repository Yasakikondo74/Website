function scroll_left() {
    var left = document.querySelector(".side_scroller");
    left.scrollBy(-350, 0)
}

function scroll_right() {
    var right = document.querySelector(".side_scroller");
    right.scrollBy(350, 0)
}